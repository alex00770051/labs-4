# labs-4
# Лабораторная работа №4
**Язык программирования:** C++  
**Студент:** Дорофеев Алексей, ИТ-6-2024  
**Вариант:** 4  

---


##  Цель работы

Освоить принципы объектно-ориентированного программирования в C++, включая:
- Создание классов с инкапсуляцией данных
- Реализацию конструкторов и методов
- Перегрузку операторов для пользовательских типов
- Обработку ошибок и безопасный ввод данных

---

##  Задание 1

### Алгоритм решения:

1. **Создать класс RightTriangle**
   - Два приватных поля: double a, double b (катеты)

2. **Реализовать три конструктора:**
   - Конструктор по умолчанию: a = 1.0, b = 1.0
   - Конструктор с параметрами: принимает a и b
   - Конструктор копирования: копирует значения из другого объекта

3. **Реализовать геттеры и сеттеры:**
   - getA(), getB() - для получения значений катетов
   - setA(), setB() - для установки значений с проверкой корректности

4. **Реализовать основной метод calculateArea():**
   - Возвращает (a * b) / 2.0 - площадь прямоугольного треугольника
   - Результат типа double

5. **Реализовать метод проверки корректности:**
   - isValid() - проверяет, что оба катета положительные

6. **Перегрузить оператор вывода <<:**
   - Выводит информацию о треугольнике в формате: "a=X, b=Y, площадь=Z"

7. **Реализовать безопасный ввод данных:**
   - Функция safeInput() с проверкой корректности ввода

---

##  Задание 2

### Алгоритм решения:

1. **Добавить унарные операции:**
   - operator++() - увеличивает стороны треугольника в 2 раза
   - operator--() - уменьшает стороны треугольника в 2 раза
   - Реализовать префиксную и постфиксную формы

2. **Реализовать операции приведения типа:**
   - explicit operator double() - возвращает площадь треугольника или -1 если треугольник не существует
   - operator bool() - возвращает true если треугольник существует, false в противном случае

3. **Добавить бинарные операции:**
   - operator<=(RightTriangle t) - сравнивает площади треугольников
   - operator>=(RightTriangle t) - сравнивает площади треугольников

4. **Создать демонстрационную программу:**
   - Меню для тестирования всех операций
   - Ввод данных с клавиатуры
   - Наглядное отображение результатов

---

##  Структура программы

### Файлы проекта:
- **header.h** - заголовочный файл с объявлением класса
- **source.cpp** - реализация методов класса
- **main.cpp** - демонстрационная программа с меню

### Основные компоненты класса:

**Поля:**
- double a - первый катет
- double b - второй катет

**Методы:**
- Конструкторы (3 вида)
- Геттеры: getA(), getB()
- Сеттеры: setA(), setB() 
- calculateArea() - вычисление площади
- isValid() - проверка корректности треугольника

**Перегруженные операторы:**
- Унарные: ++, --
- Приведения: double, bool
- Бинарные: <=, >=
- Вывода: <<

---

## 💻 Реализация
```

#include "RightTriangle.h"
#include <limits>
#include <cmath>

// Конструктор по умолчанию
RightTriangle::RightTriangle() : a(1.0), b(1.0) {}

// Конструктор с параметрами
RightTriangle::RightTriangle(double a_val, double b_val) : a(a_val), b(b_val) {
    if (!isValid()) {
        a = 1.0;
        b = 1.0;
        std::cout << "Некорректные данные! Установлены значения по умолчанию (1.0, 1.0)" << std::endl;
    }
}

// Конструктор копирования
RightTriangle::RightTriangle(const RightTriangle& other) : a(other.a), b(other.b) {}

// Геттеры
double RightTriangle::getA() const { return a; }
double RightTriangle::getB() const { return b; }

// Сеттеры с проверкой корректности
void RightTriangle::setA(double newA) {
    if (newA > 0) {
        a = newA;
    } else {
        std::cout << "Ошибка: катет должен быть положительным числом!" << std::endl;
    }
}

void RightTriangle::setB(double newB) {
    if (newB > 0) {
        b = newB;
    } else {
        std::cout << "Ошибка: катет должен быть положительным числом!" << std::endl;
    }
}

// Проверка корректности треугольника
bool RightTriangle::isValid() const {
    return a > 0 && b > 0;
}

// Основной метод - вычисление площади треугольника
double RightTriangle::calculateArea() const {
    return (a * b) / 2.0;
}

// Перегрузка оператора вывода
std::ostream& operator<<(std::ostream& os, const RightTriangle& triangle) {
    os << "a=" << triangle.a << ", b=" << triangle.b << ", площадь=" << triangle.calculateArea();
    return os;
}

// Унарные операции
RightTriangle& RightTriangle::operator++() {
    a *= 2;
    b *= 2;
    return *this;
}

RightTriangle RightTriangle::operator++(int) {
    RightTriangle temp = *this;
    a *= 2;
    b *= 2;
    return temp;
}

RightTriangle& RightTriangle::operator--() {
    a /= 2;
    b /= 2;
    return *this;
}

RightTriangle RightTriangle::operator--(int) {
    RightTriangle temp = *this;
    a /= 2;
    b /= 2;
    return temp;
}

// Операции приведения типа
RightTriangle::operator double() const {
    return isValid() ? calculateArea() : -1.0;
}

RightTriangle::operator bool() const {
    return isValid();
}

// Бинарные операции
bool RightTriangle::operator<=(const RightTriangle& other) const {
    return calculateArea() <= other.calculateArea();
}

bool RightTriangle::operator>=(const RightTriangle& other) const {
    return calculateArea() >= other.calculateArea();
}

// Функция для безопасного ввода числа
double safeInput(const std::string& prompt) {
    double value;
    while (true) {
        std::cout << prompt;
        std::cin >> value;
        if (std::cin.fail() || value <= 0) {
            std::cin.clear();
            std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
            std::cout << "Ошибка! Введите положительное число: ";
        } else {
            break;
        }
    }
    return value;
}
```

### Особенности реализации:

**Сеттеры и геттеры используются для:**
- Контролируемого доступа к приватным полям
- Проверки корректности данных при установке значений
- Обеспечения инкапсуляции данных

**Сеттеры выполняют проверку:**
- Если передано отрицательное значение, устанавливается значение по умолчанию (1.0)
- Выводится сообщение об ошибке

**Геттеры предоставляют:**
- Безопасный доступ к значениям катетов
- Возможность получения данных без нарушения инкапсуляции

---

##  Тестирование

```
Выберите треугольник для изменения (1 или 2): 1

--- СОЗДАНИЕ ТРЕУГОЛЬНИКА ---
Введите катет a: 5
Введите катет b: 9
Создан: a=5, b=9, площадь=22.5
```

```
Ошибка! Введите число от 0 до 9: }2
Ошибка! Введите число от 0 до 9: 2

--- ТЕКУЩИЕ ТРЕУГОЛЬНИКИ ---
Треугольник 1: a=5, b=9, площадь=22.5
Треугольник 2: a=5, b=12, площадь=30
```

```
Выберите треугольник (1 или 2): 1

--- ОПЕРАЦИЯ ++ (УВЕЛИЧЕНИЕ В 2 РАЗА) ---
Исходный треугольник: a=5, b=9, площадь=22.5
Префиксный ++: a=10, b=18, площадь=90
Постфиксный ++: После операции: a=10, b=18, площадь=90
Возвращенное значение: a=5, b=9, площадь=22.5
```

```
Выберите треугольник (1 или 2): 1

--- ОПЕРАЦИЯ -- (УМЕНЬШЕНИЕ В 2 РАЗА) ---
Исходный треугольник: a=10, b=18, площадь=90
Префиксный --: a=5, b=9, площадь=22.5
Постфиксный --: После операции: a=5, b=9, площадь=22.5
Возвращенное значение: a=10, b=18, площадь=90
```
```
--- ЯВНОЕ ПРИВЕДЕНИЕ К DOUBLE (ПЛОЩАДЬ) ---
Треугольник 1: a=5, b=9, площадь=22.5
static_cast<double>(t1) = 22.5
Треугольник 2: a=5, b=12, площадь=30
static_cast<double>(t2) = 30
Некорректные данные! Установлены значения по умолчанию (1.0, 1.0)
Некорректный треугольник: a=1, b=1, площадь=0.5
static_cast<double>(invalid) = 0.5
```

```
--- НЕЯВНОЕ ПРИВЕДЕНИЕ К BOOL (СУЩЕСТВОВАНИЕ) ---
Треугольник 1: a=5, b=9, площадь=22.5
bool(t1) = true
Треугольник 2: a=5, b=12, площадь=30
bool(t2) = true

Использование в условиях:
Треугольник 1 существует и может использоваться
Треугольник 2 существует и может использоваться
```

```
--- СРАВНЕНИЕ ТРЕУГОЛЬНИКОВ ---
Треугольник 1: a=5, b=9, площадь=22.5
Треугольник 2: a=5, b=12, площадь=30

Результаты сравнения:
t1 <= t2: true
t1 >= t2: false
t2 <= t1: false
t2 >= t1: true
```

```
--- ТЕСТИРОВАНИЕ КОНСТРУКТОРОВ ---
1. Конструктор по умолчанию:
RightTriangle t1; -> a=1, b=1, площадь=0.5

2. Конструктор с параметрами:
RightTriangle t2(5.0, 12.0); -> a=5, b=12, площадь=30

3. Конструктор копирования:
RightTriangle t3(t2); -> a=5, b=12, площадь=30

4. Конструктор с некорректными данными:
Некорректные данные! Установлены значения по умолчанию (1.0, 1.0)
RightTriangle t4(-1.0, 0.0); -> a=1, b=1, площадь=0.5
```

```
--- ТЕСТИРОВАНИЕ ГЕТТЕРОВ И СЕТТЕРОВ ---
Исходный треугольник: a=3, b=4, площадь=6

Использование геттеров:
triangle.getA() = 3
triangle.getB() = 4

Использование сеттеров (корректные данные):
После setA(6.0) и setB(8.0): a=6, b=8, площадь=24

Использование сеттеров (некорректные данные):
Ошибка: катет должен быть положительным числом!
Ошибка: катет должен быть положительным числом!
После попытки setA(-5.0) и setB(0.0): a=6, b=8, площадь=24
```

---

## Вывод

В ходе выполнения лабораторной работы:

1. **Реализован класс RightTriangle**, полностью соответствующий требованиям задания
2. **Использованы геттеры и сеттеры** для контролируемого доступа к данным
3. **Освоены принципы ООП**: инкапсуляция, конструкторы, методы
4. **Реализована перегрузка операторов**: унарных, бинарных, приведения типов
5. **Обеспечена безопасность работы**: проверка корректности данных, обработка ошибок ввода
6. **Создан дружественный интерфейс**: удобное меню для тестирования всех функций

**Сеттеры и геттеры являются важной частью реализации**, так как они:
- Обеспечивают инкапсуляцию данных
- Позволяют контролировать целостность объекта
- Предоставляют безопасный интерфейс для работы с классом
